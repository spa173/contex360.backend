import { it } from 'vitest';
import { ThirdPartiesService } from './third-parties.service';

describe('ThirdPartiesService', () => {
  it('should be defined', () => {
    expect(ThirdPartiesService).toBeDefined();
  });
});
